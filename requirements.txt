# PyTorch (GPU - CUDA 12.1). Install separately:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Core (GPU profiling)
psutil>=5.9.0
pandas>=2.0.0
numpy>=1.24.0

# IMDB Deduplicator (semantic similarity)
sentence-transformers>=2.2.0
faiss-cpu>=1.7.0

# Evaluation pipeline
transformers>=4.30.0
scikit-learn>=1.2.0
tqdm>=4.65.0

# AI Explanations (optional - set GEMINI_API_KEY)
google-generativeai>=0.3.0
